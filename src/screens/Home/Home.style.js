import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  imgContainer: {
    width: '100%',
    height: 500,
    resizeMode: 'cover',
    alignItems: 'center',
  },
  textWellCome: {
    fontSize: 80,
    fontWeight: 'bold',
    color: 'white',
  },
});

export default styles;
